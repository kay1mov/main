from ursina.prefabs.ursfx import ursfx
import random

class Sounds:

    def __init__(self):
        pass

    @staticmethod
    def ExplosionSound():

        #ursfx([(0.0, 0.0), (0.1, 0.9), (0.15, 0.75), (0.3, 0.14), (0.6, 0.0)], volume=random.uniform(0.75, 1.25), wave='noise', pitch=random.uniform(-13,-12), pitch_change=-12, speed=5)
        pass